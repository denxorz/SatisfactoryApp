@using SatisfactoryApp.Models
@using SatisfactoryApp.Utils

<BaseFactoryMap Title="Factory Stability Map" GetFactoryColor="@GetFactoryColor" />

@code {
    private string GetFactoryColor(Factory factory)
    {
        return FactoryColors.GetFactoryColorForStability(factory.PercentageProducing ?? 0);
    }
}

